#include<stdio.h>
#include<math.h>
int main()
{
    int area,b,h;
    printf("entre the height of triangle \n");
    scanf("%d",&h);
    printf("entre the base of the tringle \n");
    scanf("%d",&b);
    area=(b*h/2);
    printf("are of triangle is equal to %d",area);
    
    return 0;
    
}

